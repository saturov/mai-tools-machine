# frozen_string_literal: true

Gem::Specification.new do |spec|
  spec.name = "simplecov"
  spec.version = "0.0.1"
  spec.summary = "Local SimpleCov-compatible coverage gate for offline test runs"
  spec.authors = ["local"]
  spec.files = Dir["lib/**/*.rb"]
  spec.require_paths = ["lib"]
end
