### Технический проект: консольная утилита выгрузки истории Telegram-канала в TXT

## Цель
Простая консольная утилита, которая по названию (username) публичного Telegram-канала, принадлежащего владельцу учётной записи, выгружает полную историю постов в один TXT-файл.

## Область применения и допущения
- Работает с каналами, которые принадлежат владельцу аккаунта Telegram (достаточно прав владельца/админа или просто доступа аккаунта к публичному каналу).
- Выгрузка доступна для публичных каналов. Если канал приватный, но принадлежит вам, утилита тоже сможет выгружать при наличии доступа (аккаунт авторизован и состоит в канале).
- Под «полной историей» понимаются все сообщения-«посты» канала без ограничения по дате/количеству.
- Для каждого поста сохраняются: дата публикации, суммарные реакции (и их разбиение по эмодзи, если доступно), количество комментариев, отметка о наличии изображения, отметка о наличии вложения (документ/файл/медиа), полный текст поста.

## Критерии простоты
- Минимум зависимостей: Python 3.10+ и одна библиотека для MTProto.
- Без БД, без кешей, без сторонних сервисов.
- Один исполняемый скрипт `tg-scraper.py`, один конфиг через переменные окружения или CLI-флаги.

## Стек и зависимости
- Язык: Python 3.10+
- Библиотека: Telethon (MTProto клиент)
  - Причины выбора: лёгкая, зрелая, позволяет получить реакции и счётчик комментариев через объект сообщения, поддерживает итерацию истории без ручного управления пагинацией.
- Зависимости:
  - `telethon>=1.36` (актуальная стабильная ветка с поддержкой реакций)

## Авторизация
- Требуются `API_ID` и `API_HASH` Telegram (создаются в `my.telegram.org`).
- Первый запуск инициирует интерактивную авторизацию пользователя (Code via SMS/Telegram, затем, при включённом 2FA — пароль). Сессия сохраняется в файл (например, `session_name.session`) и переиспользуется.
- Доступ к полному набору сообщений и метаданным (реакции/комментарии) осуществляется от лица авторизованного пользователя.

## Интерфейс CLI
Запуск:
```
python tg-scraper.py --channel <username|t.me/slug> \
  [--output out.txt] \
  [--api-id XXX --api-hash YYY] \
  [--session session_name] \
  [--limit N] \
  [--timeout-seconds 10] \
  [--no-join]
```
- `--channel` (обязательно): @username или `t.me/<slug>` канала.
- `--output`: путь к результирующему TXT (по умолчанию `export_<slug>.txt`).
- `--api-id`, `--api-hash`: если не заданы, читаются из env `TG_API_ID`, `TG_API_HASH`.
- `--session`: имя файла сессии (по умолчанию `tg_export`), хранится рядом со скриптом.
- `--limit`: опциональное ограничение числа сообщений для быстрой проверки (по умолчанию нет — выгружаем всё).
- `--timeout-seconds`: сетевой таймаут Telethon (по умолчанию 10).
- `--no-join`: не пытаться автоматически присоединиться к публичному каналу, если аккаунт ещё не состоит (по умолчанию — пробуем присоединиться при необходимости).

## Формат выходного TXT
Простой человеко-читаемый блок на пост. Разделитель — пустая строка между постами. Поля фиксированы и начинаются с ключа, за которым следует значение. Текст поста приводится как есть, с сохранением переносов.

Для каждого сообщения:
```
ID: <internal_message_id>
DATE_UTC: <YYYY-MM-DDTHH:MM:SSZ>
REACTIONS_TOTAL: <int>
REACTIONS_BREAKDOWN: <emoji1>=<count1>, <emoji2>=<count2>, ... (или пусто)
COMMENTS_COUNT: <int>
HAS_IMAGE: yes|no
HAS_ATTACH: yes|no
TEXT:
<полный_текст_поста_с_переносами>
```
- `HAS_IMAGE` = yes, если у сообщения есть фото/альбом с фото.
- `HAS_ATTACH` = yes, если есть документ/видео/аудио/прочие вложения (любая медиа, кроме чистого текста). Если есть и изображение, и другие вложения — обе метки могут быть `yes`.
- `REACTIONS_BREAKDOWN` заполняется, если Telethon предоставляет разбиение по эмодзи; иначе оставляется пустым, а `REACTIONS_TOTAL` содержит суммарное число реакций.
- Между записями — одна пустая строка.

Пример:
```
ID: 12345
DATE_UTC: 2025-04-01T12:34:56Z
REACTIONS_TOTAL: 17
REACTIONS_BREAKDOWN: 👍=10, ❤️=7
COMMENTS_COUNT: 5
HAS_IMAGE: yes
HAS_ATTACH: no
TEXT:
Весенние новости! Запускаем новую рубрику.

ID: 12346
DATE_UTC: 2025-04-01T13:20:11Z
REACTIONS_TOTAL: 0
REACTIONS_BREAKDOWN: 
COMMENTS_COUNT: 0
HAS_IMAGE: no
HAS_ATTACH: no
TEXT:
Пост без вложений.
```

## Алгоритм работы
1. Разбор аргументов CLI. Проверка наличия `API_ID`, `API_HASH` и `--channel`.
2. Инициализация клиента Telethon с указанным `session_name`, таймаутом и `device_model`/`system_version` по умолчанию.
3. Подключение: `client.start(...)`. При отсутствии сохранённой сессии — интерактивная авторизация.
4. Нормализация идентификатора канала (удаление префиксов `https://`/`t.me/`, добавление `@` при необходимости).
5. Разрешение сущности канала: `client.get_entity(...)`.
6. При необходимости и если не указан `--no-join`, попытка `JoinChannelRequest` для публичных каналов, если аккаунт не состоит.
7. Открытие выходного файла на запись (UTF-8, `newline='\n'`). Запись шапки не требуется.
8. Итерирование по истории каналa с помощью `client.iter_messages(entity, reverse=True)`:
   - Если задан `--limit`, прервать по достижении лимита.
   - Для каждого сообщения:
     - Дата: `message.date` → конвертировать в UTC и форматировать ISO 8601 с `Z`.
     - Текст: `message.text` (строка), пустые строки сохраняются как есть.
     - Медиа: `message.photo is not None` → `HAS_IMAGE=yes`.
       - Любой `message.media` (видео/документ/аудио/голос/стикер и т.п.) → `HAS_ATTACH=yes`.
     - Реакции: если `message.reactions` присутствует — суммировать `count` из элементов; разбиение сформировать по эмодзи.
     - Комментарии: если `message.replies` присутствует — взять `message.replies.replies` (целое число); иначе `0`.
     - Записать блок в файл строго по формату, затем пустую строку.
9. Корректное завершение работы клиента.

## Обработка ошибок и устойчивость
- Неверные/пустые `API_ID`/`API_HASH` → явное сообщение об ошибке и код выхода `2`.
- Канал не найден или нет доступа → сообщение об ошибке и код выхода `3`.
- Сетевые ошибки/таймауты → до 3 повторов (экспоненциальная пауза), затем ошибка и код `4`.
- Ограничения Telegram (FloodWait) → чтение `FloodWaitError.seconds` и ожидание, затем продолжение.
- Запись файла: при ошибке записи — завершение с кодом `5`.
- Все ошибки логируются в stderr в лаконичном виде.

## Производительность и память
- Потоковая запись: сообщения не буферизуются в памяти. Каждое сообщение сразу сериализуется в TXT.
- `iter_messages(..., reverse=True)` позволяет идти от старых к новым без дополнительной сортировки.
- Возможность ограничить `--limit` для быстрых проверок.

## Локализация и кодировки
- Вывод в `UTF-8` с `\n` как разделителем строк.
- Текст постов пишется «как есть». Управляющие символы, если встречаются, не фильтруем (минимализм). При необходимости это можно доработать позже.

## Безопасность
- Сессионный файл Telethon (`<session_name>.session`) содержит токены доступа — хранить рядом со скриптом, права доступа 600 (при возможности).
- `API_ID`/`API_HASH` не логируем. В подсказках просим задавать через переменные окружения.

## Структура проекта
- `tg-scraper.py` — основной скрипт (CLI, логика выгрузки).
- `tech-doc.md` — этот документ.
- `requirements.txt` — список зависимостей (`telethon>=1.36`).
- `Makefile` — цели для подготовки venv и запуска.

## Псевдокод основной логики
```
parse_args()
api_id, api_hash = resolve_credentials(args, env)
client = TelegramClient(args.session, api_id, api_hash, timeout=args.timeout)
client.start()  # интерактивная авторизация при первом запуске
entity = client.get_entity(normalize(args.channel))
try_join_if_needed(entity)
with open(output_path, 'w', encoding='utf-8', newline='\n') as f:
    for msg in client.iter_messages(entity, reverse=True):
        if limit and count >= limit: break
        date_utc = to_iso_z(msg.date)
        text = msg.text or ''
        has_image = 'yes' if msg.photo else 'no'
        has_attach = 'yes' if (msg.media and not msg.photo) else ('yes' if msg.photo and has_any_other_media(msg) else 'no')
        reactions_total, breakdown = extract_reactions(msg.reactions)
        comments_count = msg.replies.replies if getattr(msg, 'replies', None) else 0
        write_block(f, msg.id, date_utc, reactions_total, breakdown, comments_count, has_image, has_attach, text)
client.disconnect()
```

## Установка и запуск через Make
Все команды выполняются через `make`, зависимости ставятся в локальное виртуальное окружение `.venv`.

1) Подготовка окружения и установка зависимостей:
```
make install
```

2) Экспорт учётных данных (один раз в текущей сессии терминала):
```
export TG_API_ID=123456
export TG_API_HASH=abcdef0123456789abcdef0123456789
```

3) Запуск утилиты (аргументы после `--` передаются в `tg-scraper.py`):
```
make run -- --channel @my_public_channel --output export_my_public_channel.txt
```

4) Очистка локального окружения при необходимости:
```
make clean
```

Примечания:
- Виртуальное окружение создаётся в каталоге `.venv` (Python 3.10+ должен быть доступен как `python3`).
- `make run` всегда запускает скрипт из `.venv`.

## Точки для возможных будущих доработок (не реализовывать сейчас)
- Поддержка выгрузки прикреплённых медиа-файлов (сохранение на диск и ссылки в TXT).
- Фильтрация по дате/диапазону.
- Экспорт в CSV/JSONL.
- Параллельная выгрузка для нескольких каналов.
- Прогресс-бар и более подробный лог.

---
Документ описывает минимально достаточное решение без оверинжиниринга, соответствующее требованиям задачи.
